[project]
name = "instagram-osint-mcp"
version = "0.1.0"
description = "MCP server for evidence-grade Instagram OSINT capture"
requires-python = ">=3.11"

dependencies = [
  "mcp>=1.0.0",
  "playwright>=1.45.0",
]

[project.scripts]
instagram-mcp = "instagram_mcp.server:main"
